# Redis master / replica configuration for the optional 'sentinel' Docker Compose profile.
# Replicas are started with --replicaof redis-master 6379 passed on the command line,
# so this file is shared by both roles.

# (dev) no persistence — keeps the sentinel topology disposable
save ""
appendonly no
